# Frontend env — copy to .env
# NOTE: the Groq key is NOT set here anymore. All LLM calls are proxied through
# the server (/api/groq/chat) so the key never reaches the browser. The frontend
# only needs to know where the backend is.
VITE_API_URL=http://localhost:3001
